<div class="min-h-screen bg-gray-50">
  <header class="bg-white border-b border-gray-200 shadow-sm">
    <div class="max-w-5xl mx-auto px-8 py-5 flex items-center gap-4">
      <%= link_to root_path, class: "text-gray-400 hover:text-gray-600 transition-colors" do %>
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
        </svg>
      <% end %>
      <div class="w-10 h-10 rounded-xl flex items-center justify-center" style="background:#6366f1;">
        <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"/>
        </svg>
      </div>
      <div>
        <h1 class="text-xl font-bold text-gray-900">特化型AI</h1>
        <p class="text-xs text-gray-400">キーワードを入れるだけで精度の高い回答が得られます</p>
      </div>
    </div>
  </header>

  <main class="max-w-5xl mx-auto px-8 py-10">
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-5">
      <% @specialties.each do |specialty| %>
        <%= link_to ai_specialty_path(specialty),
              class: "group block bg-white rounded-2xl border border-gray-200 shadow-sm hover:shadow-md transition-all duration-200 overflow-hidden",
              style: "--accent: #{specialty.color};" do %>
          <div class="p-6">
            <div class="w-12 h-12 rounded-xl flex items-center justify-center mb-4 text-2xl transition-transform group-hover:scale-110 duration-200"
                 style="background:<%= specialty.bg_color %>;">
              <%= specialty.icon_emoji %>
            </div>
            <h3 class="font-semibold text-gray-800 text-base group-hover:text-[var(--accent)] transition-colors">
              <%= specialty.name %>
            </h3>
            <p class="text-sm text-gray-500 mt-1"><%= specialty.description %></p>
          </div>
          <div class="px-6 pb-5 flex items-center gap-1 text-sm font-medium opacity-0 group-hover:opacity-100 transition-opacity"
               style="color:<%= specialty.color %>;">
            <span>使ってみる</span>
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/>
            </svg>
          </div>
        <% end %>
      <% end %>
    </div>
  </main>
</div>
